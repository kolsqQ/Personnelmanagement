<Page x:Class="Personnelmanagement.View.EmployeeListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Personnelmanagement.View"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="430"
      Title="EmployeeListPage">


    <Grid>
        <DataGrid CanUserResizeColumns="False" CanUserResizeRows="False"  SelectionUnit="FullRow" x:Name="EmployeeGrid" SelectionChanged="DataGrid_SelectionChanged" AutoGenerateColumns="False" Margin="0,0,0,50" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding id}" Width="1" Header="id" />
                <DataGridTextColumn Binding="{Binding Name}" Width="150" Header="Имя" />
                <DataGridTextColumn Binding="{Binding Position}" Width="125" Header="Должность"/>
                <DataGridTextColumn Binding="{Binding Phone}" Width="110" Header="Телефон"/>
            </DataGrid.Columns>

        </DataGrid>

        <StackPanel VerticalAlignment="Bottom">
            <Button Content="Редактировать" Height="50" Click="Button_Click"></Button>
            
        </StackPanel>
    </Grid>
</Page>
